const styles = theme => ({
  listItem: {
    padding: theme.spacing(1, 0),
    textAlign: "left"
  },
  total: {
    fontWeight: "700"
  },
  title: {
    marginTop: theme.spacing(2)
  },
  fab: {
    margin: theme.spacing(1)
  }
});
export default styles;
